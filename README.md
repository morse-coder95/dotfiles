# dotfiles
Generator for configuration files between home and work environments



1) Run python compile.py to generate compiled dot files

2) Run setup/configure.sh

3) Launch neovim and run:
  - :PlugInstall
  - :TSInstall lua python
  - :UpdateRemotePlugins
